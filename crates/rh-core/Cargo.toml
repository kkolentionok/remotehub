[package]
name = "rh-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core domain types, traits, and errors for RemoteHub"

[dependencies]
serde.workspace = true
serde_json.workspace = true     # used in store::SettingsStore::save for partial updates
thiserror.workspace = true
chrono.workspace = true
ulid.workspace = true
zeroize.workspace = true
async-trait.workspace = true

# Note: `tracing` and `bytes` are NOT pulled in here. rh-core is pure
# data and traits; logging happens in caller crates, and we don't
# manipulate byte streams directly.
